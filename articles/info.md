Info. 

> Big O notation. 

```
Describe the runtime of a program based on how quickly it grows relative to the input. 
```

> Random Access Memory

```
Keep tracks of numbers variables strings. 
Called as a working memory. 
Can imagine like a shelve with numbers. (each has an address). 
one slot in a shelve can store 8 bits of information. (1 byte).

```
>Processor

```
Processor is the one doing real work.
Processor is connector to the memory controller. 
Memory controller reads and writes from and to the RAM. 

Processor <-> Memory Controller <-> RAM

Processor has a local cache. Storing recently read stuff from the RAM. 
```
